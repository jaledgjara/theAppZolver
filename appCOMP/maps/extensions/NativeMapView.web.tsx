import { View } from "react-native";

/**
 * NativeMapView (Web Stubs) — Safe replacements for react-native-maps on web.
 * These render empty Views so the app compiles without native dependencies.
 */
export const MapView = View;
export const Circle = View;
export const Marker = View;
